import { createTheme} from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#35363B', 
    },
    secondary: {
      main: '#35363B',
    },
  },
});



export default theme;